#include <bits/stdc++.h>

using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define ff first
#define ss second
typedef long long ll;
typedef unsigned long long ull;

void solve() {
    ll x,y;
    cin >> x >> y;

    ll c=0;
    for(ll i=63;i>=0;i--) {
        if(((x>>i)&1) != ((y>>1)&1)) {
            
        }
    }

    cout << x1+x2 << '\n';
}

int main() { _
    int t;
    cin >> t;
    while(t--) solve();
    return 0;
}
