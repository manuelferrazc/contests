#include <bits/stdc++.h>

using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define ff first
#define ss second
typedef long long ll;
typedef unsigned long long ull;

vector<vector<int>> v;

ll fill(ll x1,ll x2,ll y1,ll y2) {
    
}

void solve() {
    ll n,q;
    cin >> n >> q;

}

int main() { _
    int t;
    cin >> t;
    while(t--) solve();
    return 0;
}
