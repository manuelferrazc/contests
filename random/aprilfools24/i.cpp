#include <bits/stdc++.h>

using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define ff first
#define ss second
typedef long long ll;
typedef unsigned long long ull;


int main() { _
    //5 shons 7 tes 12 leshas 3 shas 1 les 2 has 
    map<string,int> m;
    m.insert({"le",1});
    m.insert({"sha",2});
    m.insert({"shon",5});
    m.insert({"ta",6});
    m.insert("te",7);
    return 0;
}
